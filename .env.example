# DIGITAL SHADOW - ENVIRONMENT TEMPLATE
# Copy this to .env.local and populate with your credentials

# --- FEATURE FLAGS ---
# Set to 'true' to use mock personas instead of real API calls (Recommended for initial demo)
ENABLE_MOCK_MODE=true

# --- EXTERNAL APIs ---
# Get yours at https://rapidapi.com/ (Search for BreachDirectory or Similar)
# and https://serper.dev/ for search results
RAPIDAPI_KEY=your_rapidapi_key_here
SERPER_API_KEY=your_serper_key_here
GEMINI_API_KEY=your_gemini_api_key_here

# --- AUTHENTICATION (NEXTAUTH) ---
# Create at https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Generate a strong secret: openssl rand -base64 32
NEXTAUTH_SECRET=your_nextauth_secret_here
NEXTAUTH_URL=http://localhost:3000

# --- NARRATOR SERVICE ---
# The base URL for the narrator API (relative or absolute)
NEXT_PUBLIC_API_URL=http://localhost:3000
